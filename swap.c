#include<stdio.h>
int main() {
      double first, second;
      printf("Enter first number: ");
      scanf("%lf", &first);
      printf("Enter second number: ");
      scanf("%lf", &second);

      

      printf("\nAfter swapping, firstNumber = %.2lf\n",second);
      printf("After swapping, secondNumber = %.2lf", first);
      return 0;
}
